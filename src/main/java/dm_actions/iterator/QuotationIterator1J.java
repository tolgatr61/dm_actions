package dm_actions.iterator;

public class QuotationIterator1J extends AbstractQuotationIterator {
    // Nothing to change compared to the abstraction, but it becomes now a concrete class
    public QuotationIterator1J(String fileName) {
        super(fileName);
    }
}
